{% doc %}
  Cart Drawer Header Component

  Renders the header section of the cart drawer with title and close button.

  @example
  {% render 'cart-drawer-header' %}
{% enddoc %}

<div class="flex flex-row items-center w-full justify-between px-5 pt-3 pb-2 border-b border-neutral-200">
  <p class="text-sm font-medium">Košarica</p>
  <button
    type="button"
    onclick="this.closest('cart-drawer').close()"
    aria-label="Zatvori"
    class="text-neutral-600 p-1 hover:text-neutral-900"
  >
    <span>
      {{- 'icon-x.svg' | inline_asset_content -}}
    </span>
  </button>
</div>
