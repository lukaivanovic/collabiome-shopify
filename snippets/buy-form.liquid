{% doc %}
  Buy Form (legacy wrapper)

  Delegates to product-buy-form for unified behavior. Kept for backward compatibility.
{% enddoc %}

{% render 'product-buy-form',
  product: product,
  section_id: 'legacy-buy-form',
  product_form_id: 'legacy-buy-form-form',
  show_dynamic_checkout: false,
  gift_card_recipient_feature_active: false,
  quantity_rule_soldout: false,
  button_variant: 'default',
  enable_free_delivery_label: false,
  free_delivery_threshold: 5000
%}
